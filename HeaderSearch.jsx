var React = require( 'react' );

var HeaderSearch = React.createClass( {
  render: function () {
    return (
    <div>
      this is a sandbox
    </div>
    );
  }
} );

module.exports = HeaderSearch;
